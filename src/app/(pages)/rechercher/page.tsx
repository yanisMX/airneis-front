'use client';
import SearchBar from '@/app/components/SearchBar';

const SearchPage = () => {
  return (
    <div className="min-h-screen">
      <SearchBar />
    </div>
  );
};

export default SearchPage;
